import { Head, Link } from '@inertiajs/react';
import AdminLayout from '@/Layouts/AdminLayout';
function Badge({ value }) { const tone = value === 'registration_open' ? 'bg-emerald-100 text-emerald-800' : 'bg-neutral-100 text-neutral-700'; return <span className={`inline-flex rounded px-2 py-1 text-[10px] font-bold uppercase tracking-wider ${tone}`}>{value === 'registration_open' ? 'Pendaftaran dibuka' : value || '-'}</span>; }
export default function Index({ programs, mbkmPrograms, applications = [] }) { const rows = (programs || mbkmPrograms || { data: [] }).data || programs || mbkmPrograms || []; const submitted = applications.data || applications || []; return <AdminLayout title="MBKM"><Head title="MBKM" /><div className="min-h-dvh space-y-6 bg-neutral-50 p-6"><div className="bg-gradient-to-r from-brand-900 via-brand-800 to-brand-700 p-6 text-white"><p className="text-xs font-bold uppercase tracking-widest">Merdeka Belajar</p><h1 className="mt-1 text-2xl font-bold">Program MBKM</h1><p className="mt-1 text-sm text-white/80">Temukan program di luar kampus dan ajukan pendaftaran Anda.</p></div>{submitted.length > 0 && <section className="border bg-white p-4"><h2 className="mb-3 text-sm font-bold uppercase">Pendaftaran Saya</h2><div className="space-y-2">{submitted.map(application => <Link key={application.id} href={`/mahasiswa/mbkm/applications/${application.id}`} className="flex items-center justify-between border p-3 text-sm hover:bg-neutral-50"><span><b>{application.program?.name}</b><span className="ml-2 text-xs text-neutral-500">{application.application_number}</span></span><Badge value={application.status} /></Link>)}</div></section>}<section><h2 className="mb-3 text-sm font-bold uppercase text-neutral-700">Program Tersedia</h2><div className="grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3">{rows.map(program => <article key={program.id} className="flex flex-col border bg-white p-5"><div className="flex items-start justify-between gap-2"><p className="text-xs font-bold text-neutral-500">{program.code}</p><Badge value={program.status} /></div><h3 className="mt-3 text-lg font-bold">{program.name}</h3><p className="mt-1 text-sm text-neutral-600">{program.program_type?.name || program.mbkm_program_type?.name || 'Program MBKM'}</p><dl className="mt-4 space-y-1 text-xs text-neutral-500"><div><dt className="inline">Pendaftaran: </dt><dd className="inline">{program.registration_start} s/d {program.registration_end}</dd></div><div><dt className="inline">Kuota: </dt><dd className="inline">{program.quota ?? 'Tidak dibatasi'}</dd></div></dl><Link href={`/mahasiswa/mbkm/programs/${program.id}`} className="mt-5 w-fit bg-black px-3 py-2 text-xs font-bold text-white">Lihat & Daftar</Link></article>)}{rows.length === 0 && <p className="col-span-full border border-dashed bg-white p-8 text-center text-sm text-neutral-500">Belum ada program MBKM yang membuka pendaftaran.</p>}</div></section></div></AdminLayout>; }
